<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background>
        <inset android:drawable="@mipmap/ic_launcher_background" android:inset="0%" />
    </background>
    <foreground>
        <!-- Inset the full-bleed S into the adaptive safe zone so the launcher mask
             doesn't crop it — the logo then sits at ~68% centered with navy margin,
             matching the iOS icon instead of appearing zoomed/clipped. -->
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16%" />
    </foreground>
</adaptive-icon>