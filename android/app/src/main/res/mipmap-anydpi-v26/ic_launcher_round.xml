<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background>
        <inset android:drawable="@mipmap/ic_launcher_background" android:inset="0%" />
    </background>
    <foreground>
        <!-- Match iOS: inset the full-bleed S into the safe zone so round masks
             don't crop it (logo ~68% centered with navy margin). -->
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16%" />
    </foreground>
</adaptive-icon>