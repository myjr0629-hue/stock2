# 마케팅 사이클 런북 — 정본

**이 문서 하나로 다른 에이전트가 그대로 이어받을 수 있어야 한다.** 매 사이클 시작할 때 이 문서와 `ENGINE.md` 를 읽는다.
목적은 «게시 건수»가 아니라 **앱 설치**다. 지표는 수단이다.

---

## 0. 이 스케줄이 «실제로» 어떻게 도는가

- **크론 = 이 클로드 코드 세션 안의 반복 알람**이다. Vercel 도 서버도 아니다. `CronList` 로 확인하고 `CronCreate`/`CronDelete` 로 관리한다.
- 현재 **매시 :13 에 1개**(`c5e53356`)가 돈다. 2026-09-17 14:0x KST 에 이 하나로 정리했다(기존 `3275f760` 시간당 + `ef15ad97` 일일 1회성 두 개를 삭제). 대표님이 컴퓨터를 계속 켜 두시므로 세션이 살아 있고, 이 구조가 정상 동작 조건이다.
- **깨우는 경로는 «크론 하나»뿐이다.** ScheduleWakeup(자기 예약)은 **쓰지 않는다** — 두 개를 같이 쓰면 한 시간에 두 번 돌아 캡을 낭비하고 기록이 꼬인다. 2026-09-17 에 이 중복을 제거했다.
- **반복 크론은 7일 뒤 자동 만료된다(즉 2026-09-24 경).** 그래서 **매 사이클 1단계에서 `CronList` 로 남은 수명을 확인**하고, 만료가 가까우면 그 자리에서 새로 만든다. 이건 선택이 아니라 절차다.
- Vercel 크론 44개 중 마케팅은 **0개**다. 프로덕션 자동화는 없고 **만들지 않는다**(대표 지시).

---

## 1. 사이클 절차 — 이 순서를 지킨다

```bash
cd ~/.gemini/antigravity/scratch/stock2
node scripts/mkt-plan.js slot      # ① 담당 구역 배정 (이게 이번 시간에 할 일이다)
node scripts/audit-expiration-selection.js --live   # ② 발행 게이트
```

1. **크론 수명 확인** — `CronList`. 7일 만료가 가까우면 재생성.
2. **담당 구역 배정** — `mkt-plan.js slot`. 출력이 곧 지시다. **골라서 하지 않는다.**
   - **실행 4개**: 가장 오래 방치된 열린 채널부터. 넷 다 처리한다.
   - **뚫기 2개**: 계정이 막힌 곳. **우회로를 실제로 시도한 뒤에만** 보류로 적는다(§22).
   - **확장 1개**: 신규 표면 발굴 → 실행 또는 티켓 → `channels.json` 등록.
3. **발행 게이트** — `audit-expiration-selection.js --live`. 실패가 하나라도 있으면 **발행 금지**하고 앱부터 고친다.
4. **발행** — 채널 성격에 맞게. 모든 게시물에 **앱 화면 또는 앱 카드 + 스마트링크 `?from=<채널>`**. 단 레딧·Quora·HN 은 **무링크 순수 가치**(앱명 0~1회).
5. **발행 즉시 기록·검증** — `node scripts/mkt-plan.js pub <채널> <URL>` + **공개 페이지에서 본문·이미지·링크 확인**. 검증 못 하면 «발행했다»고 쓰지 않는다.
6. **광고** — 콘솔이 살아 있으면 **기간을 «오늘»로 고정**하고 지출·설치·CPA + 키워드 단위를 읽는다. 이긴 키워드 확장, 진 키워드 정지. **예산·입찰 증액 금지.**
7. **기록** — `OUTREACH-LOG.md` 에 **과정·결과·개선사항**을 성공과 실패 모두 적는다. 판정이 뒤집히면 그 자리에서 정정한다.
8. **커밋·푸시** — `git add <경로>` (**-A 금지**) → 커밋 → 푸시.
9. **보고** — 한글. 대표 개입이 필요한 것은 티켓으로 쌓고 **한 번에** 보고한다.

### 사이클 0-b: 대표 가입 체크를 «먼저» 본다
`mkt-plan.js slot` 이 `CEO-SIGNUP-LIST.md` 의 `- [x]` 를 읽어 **「대표님이 체크한 신규 가입 N건」**을 출력한다.
체크가 있으면 **그 사이클의 1순위**다: ①`channels.json` 에서 `enabled:true` ②그 채널 성격에 맞게 **첫 발행까지** ③원장 기록·검증 ④체크 항목을 목록에서 «완료»로 옮긴다.
대표가 아이디를 적어 주면 `channel-accounts-registry.md` 메모리에도 반영한다. **비밀번호는 요청하지도, 기록하지도 않는다.**

---

### 매 사이클 «성적표» 한 줄 — 브라우저 없이 돈다 (2026-09-19)

```bash
node scripts/check-store-ratings.js      # 종료코드 1 = 세 앱 모두 별점 0
```
**왜 이게 사이클 고정인가**: 2026-09-19 실측으로 병목이 «별점 0» 으로 확정됐다 —
Play 노출 2,190 → 등록정보 열람 약 11(**0.5%**) → 설치 7, 그런데 **열면 61% 가 설치한다.**
애플광고 US 도 같은 모양이다(노출 138 → 탭 **0**). 한국은 경쟁이 옅어 프리마켓 노출 1,339 → 탭 8 → 설치 1.
**붐비는 줄에서 별 없는 앱은 건너뛰어진다.** 별점이 붙기 전에 키워드·입찰을 늘리는 것은 돈을 노출에 쓰는 일이다.

이 숫자가 0에서 움직이면 2026-09-19 배포한 리뷰 요청 수정(사용일 2·7일 + 앱 실행 4회째, 하루 1회)이 듣는 것이다.
**브라우저가 대표 제어로 잠겨도 이 검사는 돈다** — 가장 중요한 지표를 브라우저에 묶어 두지 않는다.
관제 성과 패널 맨 위에도 같은 값이 뜬다(`node scripts/hud/collect.js --ratings`).

### 시간대 규칙 (대표 지시 2026-09-18)

| 채널 | 언제 |
|---|---|
| **네이버 블로그** | **하루 3편 · KST 08·12·16시 전후**(크론 기준 08:13·12:13·16:13). 한국 독자용이므로 **한국 밤·새벽 금지** |
| 지식인·오늘의집 등 KR | 같은 이유로 한국 낮 시간 우선 |
| X(US)·레딧·Quora·맥루머스 | **미국장 시간대**(KST 22:30~05:00)에 도달이 높다 |
| note(JP)·Qiita | 일본 낮(KST 09~22) |

「한 채널 하루 1편」은 네이버 블로그에 한해 **3편**으로 대표가 올렸다.

## 2. 절대 지키는 안전선

계정생성 · 비밀번호 입력 · 결제정보 입력 **금지** · **예산·입찰 증액 금지**(대표 승인) · 앱/웹 코드 **무수정**(스코어 제외) · 유튜브 접근 금지 · StockTwits 금지 · r/investing 앱언급 금지 · 레딧 하루 3건(UTC일)·8분 간격·본문 링크 금지·같은 스레드 중복 금지 · **한 채널 하루 1편** · 예측·투자권유 표현 금지 · 라이브 앱 변경은 실화면 검증 후에만.

---

## 3. 이미 확인된 차단 — 다시 시도해서 시간 낭비하지 말 것

| 표면 | 상태 | 확인된 근거 |
|---|---|---|
| **해커뉴스** | 게시 불가(대표 전용) | 사이트 가이드라인 「Don't post generated text or AI-edited text」 |
| **r/options·r/StockMarket·r/investing·r/iosapps** | 게시 불가 | 서브 규칙에 AI 작성물 금지 |
| **DCinside** | 게시 불가(대표 전용) | 글쓰기 화면에 `type=password` 입력란 실재(실화면 확인) |
| **StockTwits** | 무기한 제재 | House Rules 위반 배너 |
| **틱톡** | 도달 0 | 신생 계정 playCount 0, 폰 실사용 이력이 전제 |
| **5ch·야후파이낸스·민카부·지혜주머니·LINE오픈챗** | 자사링크 금지 | 각 규약 명문. 5ch 는 해외 IP 쓰기까지 차단 |
| **Appliv 무료등재** | 사망 | 掲載依頼 URL 전부 404, 유료 전용 전환 |

**단, 「막혔다」가 «방법»인지 «채널»인지 구분한다(§22).** 블루스카이는 「파일 입력 0개」를 채널 차단으로 적어 2주를 놀렸다 — 실제로는 drop+paste 로 붙었다.

---

## 4. 반복하면 안 되는 실수 (전부 내가 실제로 저질렀다)

1. **한 방법만 해 보고 «채널이 막혔다»로 적기** → 블루스카이 2주 유실. 이미지 첨부 경로는 넷이다: `setInputFiles` / `input[type=file] >> nth=0` / `drop` / `paste`. 플랫폼마다 답이 다르다.
2. **`const URL = "..."` 로 전역 URL 클래스 가리기** → `fetch` 가 전부 깨지고 try/catch 가 삼켜 «클릭 0건»이라는 거짓 판독이 나왔다.
3. **날짜 기준 혼동** → 귀속 키는 **ET 날짜**다. UTC·로컬로 조회하면 전부 0으로 보인다.
4. **`scrollIntoView` 직후 좌표 사용** → 스크롤이 끝나기 전이라 다른 행이 선택된다. 대기 후 **재측정**한다.
5. **자동완성 첫 항목을 확인 없이 Enter** → SaaSHub 에 없는 이름을 치니 무관한 제품이 선택됐다. **입력어와 정규화 일치할 때만** 선택.
6. **넓은 셀렉터로 드롭다운 클릭** → 푸터 링크를 눌러 입력한 폼이 통째로 날아갔다. **찾기와 클릭을 분리**한다.
7. **정책 문서 URL 추측** → 4개 전부 404. Zendesk 계열은 `/api/v2/help_center/articles/search.json?query=AI` 로 찾는다.
8. **광고 보고서 기간을 확인 안 하고 숫자 비교** → 30일치와 오늘치를 섞어 읽었다.
9. **자동화 상태를 과장·축소해 보고** → 크론은 «돌고 있었는데» 「자동화가 없다」로 들리게 말했다. **있는 그대로 적는다.**
10. **ego 스크립트에서 `require`** → ESM 이라 죽는다. `await import('node:fs')`.

11. **«렌더가 안 된다»고 판단하기 전에 ①섀도 DOM 딥 워커 ②스크린샷 둘 다 확인한다.** Play 콘솔을 `document.body.innerText` 로만 읽어 700~2,800자가 나오는 것을 보고 「콘솔이 고장났다」고 두 사이클에 걸쳐 8회 오진했다. 딥 워커로는 같은 페이지가 요소 2,200개였다. 대표가 보내준 스크린샷(작은 미리보기에는 다 보이는데 전체화면만 흰색)이 전제를 깼다.
12. **ego 창이 흰색으로 그려질 때가 있다 — 그때 좌표 클릭은 허공에 떨어진다.** 딥 워커로 요소를 찾아 `element.click()` 을 부르면 렌더 상태와 무관하게 먹는다.

13. **폼 필드를 «크기 순서»로 추정하지 말 것.** Uptodown 설명 탭에서 편집 필드를 높이 오름차순으로 정렬해 「가장 작은 것 = 짧은 설명」으로 가정했는데, 숨은 필드(높이 0)가 2개 섞여 있어 **기존 1,306자 설명을 덮어썼다.** 높이 10px 이상만 남긴 뒤 「60px 미만 = 짧은 설명 / 100px 이상 = 전체 설명」처럼 절대 기준으로 고른다. 그리고 **덮어쓰기 전에 기존 값의 길이를 먼저 읽는다.**

---

## 5. 성과 판정 — «발행 수»가 아니라 «클릭»으로 본다

21일 스마트링크 클릭(2026-09-17 최초 측정, ET 날짜 기준, EC2 프록시 `mkt:attr:hit:<from>:<ET날짜>`):
**seo 141 · x_us 73 · quora 56 · x_jp 29 · note 14 · medium 12 · linkedin 8 · quora_bio 7 · reddit_bio 5 · hn 5 · 나머지 7 · 합계 357**
- 투입은 클릭 비중을 따라간다. **seo·x·quora 가 84%** 를 만든다.
- 클릭 0이 7일 지속되면 투입을 절반으로 줄인다. **죽이지는 않는다.**
- **클릭은 설치가 아니다.** 설치 귀속(t102)이 붙기 전까지 클릭은 «가장 좋은 대용치»일 뿐이라고 항상 함께 적는다.

---

## 6. 읽어야 할 파일

`ENGINE.md`(교리 §1~§22) · `RUNBOOK.md`(이 문서) · `channels.json`(채널 정본) · `QUEUE.json`(티켓) · `PUBLISH-LEDGER.json`(원장) · `OUTREACH-LOG.md`(과정·결과·개선) · `DIRECTORY-LIST.md` · `press/`(보도자료 초안)
메모리: `~/.claude/projects/-Users-eunhoon-Documents-Project-recipt/memory/MEMORY.md`


---

## 7. 보고 양식 — 매 보고에 «반드시» 포함한다 (대표 지시 2026-09-17)

보고는 한글. 아래 네 블록을 빠뜨리지 않는다.

### ① 이번 사이클에 «어느 플랫폼에 무엇을» 했는가
채널명 · 한 일 · 공개 URL · 검증 여부를 한 줄씩. 검증 못 한 것은 «발행했다»고 쓰지 않는다.

### ② 기본 관리 플랫폼 리스트 (로테이션 엔진에 등록된 전부)
`node scripts/mkt-plan.js` 출력을 근거로 **가동 / 계정대기 / 관리제외**로 나눠 적는다.
그리고 **«오늘 새로 추가한 것»을 기존 것과 구분**해서 따로 표기한다.

### ③ 성과
채널별 클릭(21일, ET 기준 `mkt:attr:hit:<from>:<ET날짜>`) · 광고 지출·설치·CPA(기간을 «오늘»로 고정) · 스토어 순위 변화.
**클릭은 설치가 아니다** — 설치 귀속(t102)이 붙기 전까지 대용치라고 함께 적는다.

### ④ 대표님이 하셔야 하는 것
`CEO-SIGNUP-LIST.md` 를 갱신해서 **링크와 함께** 제시한다. 가입이 편하도록 우선순위·필요한 것·왜 필요한가를 같이 쓴다.
**스토어 콘솔·광고 콘솔 작업은 여기 넣지 않는다 — 내가 직접 한다.** 대표님 몫은 ①계정 생성 ②비밀번호 입력 ③결제정보 ④외부 메일 발송 넷뿐이다.

---

## 8. 인수인계 점검 — 다른 에이전트가 이어받을 수 있는가

새 에이전트가 이 순서로 읽으면 바로 이어받을 수 있다.
1. `RUNBOOK.md`(이 문서) — 절차·안전선·확인된 차단·반복 금지 실수·보고 양식
2. `ENGINE.md` §1~§22 — 교리. 채널별 성격과 실패 사례
3. `channels.json` — 채널 정본(가동/계정대기/제외 + 각 채널의 실무 메모)
4. `QUEUE.json` — 티켓. `type: 'mkt'` 는 내 몫, `type: 'ceo'` 는 대표 몫
5. `PUBLISH-LEDGER.json` — 발행 원장(캡 계산의 유일한 근거)
6. `OUTREACH-LOG.md` — 과정·결과·개선사항의 시간순 기록
7. `CEO-SIGNUP-LIST.md` — 대표 몫 목록(링크 포함)
8. `press/` — 보도자료 초안
9. 메모리 인덱스 `~/.claude/projects/-Users-eunhoon-Documents-Project-recipt/memory/MEMORY.md`

**기록 규칙**: 사이클마다 «한 일 · 결과 · 개선사항»을 `OUTREACH-LOG.md` 에 남긴다. 실패도 남긴다.
판정이 뒤집히면 새 글을 쓰지 말고 **원래 판정을 고친다**(§20·§21·§22 가 그렇게 만들어졌다).
같은 실수를 두 번 하면 그건 기록의 실패다 — 위 4절에 항목을 추가한다.

---

## 9. Play 콘솔 실무 — 2026-09-17 에 알아낸 것 (이것 없이는 WIM 에서 두 사이클 헤맸다)

**① 콘솔 내용은 «섀도 DOM» 안에 있다.** `document.body.innerText` 로 읽으면 700~2,800자만 보이고 릴리스 표가 «없는 것처럼» 보인다. 반드시 딥 워커로 읽는다:
```js
const walk=(root,acc)=>{const k=root.querySelectorAll?root.querySelectorAll('*'):[];for(const e of k){if(e.shadowRoot)walk(e.shadowRoot,acc);acc.push(e);}return acc;};
```
딥 워커로 세면 같은 페이지가 **요소 2,200개**다. 「콘솔이 고장났다」는 내 판단은 오진이었다.

**② 좌표 클릭보다 `element.click()` 이 확실하다.** ego 창이 흰색으로 그려질 때가 있어 좌표 클릭이 허공에 떨어진다. 딥 워커로 요소를 찾아 `.click()` 을 부르면 렌더링 상태와 무관하게 먹는다.

**③ 초안이 있으면 「Create new release」는 «무효»다.** 클릭은 되는데 이동이 없다. 그때는 **Releases 탭 → 「Edit release」** 로 들어간다. 이게 유일한 문이다.

**④ 실제 트랙 ID (프로덕션)**
| 앱 | Play 앱 ID | 프로덕션 트랙 ID |
|---|---|---|
| SIGNUM | 4974871698649706116 | (미확인) |
| Undercurrent | 4976096296089482490 | 4698411096118275380 |
| **WIM** | 4974011153222225088 | **4697728196667220385** |
편집 URL 형식: `/app/<앱ID>/tracks/<트랙ID>/releases/<n>/prepare`

**⑤ 저장은 제출이 아니다.** `Save` 뒤 `/publishing` 에서 **「Submit N change for review」 → 대화상자의 「Send changes for review」** 까지 눌러야 `Changes in review` 가 된다. 겉 버튼만 누르면 그대로 대기다.

**⑥ ego 재시작은 함부로 하지 말 것.** 작업공간 번호가 초기화되고 `listTaskSpaces()` 가 빈 배열이 된다. 그때는 `newTaskSpace('mkt','Profile 1')` 로 만든다(인자 2개 필수 — 없으면 실패).
