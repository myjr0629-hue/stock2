# ⛔ 쇼츠 측정 캐시 — «루트 .gitignore 를 건드리지 않기 위해» 여기 둔다 (2026-08-22)
#   앱=맥 / 유튜브=윈도우 로 나눠 쓰므로, 공유 파일을 만들지 않는 것이 규칙이다.
#   ⚠ 경로는 이 파일이 있는 폴더(.agent/) 기준이다.

# 레퍼런스 원본·중간 산출물 — 커밋하지 않는다 (2026-08-21)
# 남의 영상 파일이고 용량이 크다. 측정 «결과»(REF_FLEET.json 등)만 남긴다.
_fleet_cache/
_script_cache/
_meta_cache/
_voicetmp/
_vf0/
