# WIM v2 작업 로그 — 어느 머신/세션에서든 여기서 인계

> 스펙 정본 = `.agent/WIM_V2_SPEC.md` (왜/무엇). 이 파일 = 진행 상태 (어디까지/어떻게).
> 규칙: 페이즈 착수·완료 시 이 파일을 같은 커밋으로 갱신. 검증은 반드시 push → 시뮬 실화면.

## 페이즈 상태

| 페이즈 | 내용 | 상태 |
|---|---|---|
| W0 | 인프라: 주말열화 + 워밍크론 + 가드 | ✅ 완료·검증 (2026-07-13 Mac) — 프로드 dateET=07-10(금), 유닛 3개 실무버(ORCL/INTC/AMAT ±2.4~2.5%, 스파크 179~192봉), 시뮬 실화면 히어로 정상 렌더 확인. 참고: 금요일 유닛 3개는 열화가 아니라 그날 ≥1.5% 유명주가 3개였던 것 (콘텐츠 기인) |
| W1 | 홈 재설계 (차트 히어로 + 플레이 덱 + 세션 스트립) | ✅ 완료 — ko/en/ja 3언어 실화면 검증 (ja 세션 스트립 "ザラ場" 네이티브 확인, 언어누수 0). 스트릭 1줄 밴드 확인 |
| W2 | P3 레벨헌트 + P4 숫자감각 + 오버레이 언락 1단계 | ✅ 완료 — 브라우저 실플레이 E2E 검증 (2026-07-13 Mac): 헌트 3라운드 드래그·$칩·대시라인 공개·근접판정(0.6%→+20 대박)·요약·언락 배지→localStorage→홈 토스트→히어로 MAX PAIN 상시→XP 반영 / 숫자감각 실값 판정·기준·개념 한줄. **버그 1건 발견·수정**: CountUp이 rAF 스로틀 환경(백그라운드 탭·iOS 저전력)에서 0에 갇힘 → 타임아웃 폴백+값 변경 시 재애니(수정 후 42.7% 정상 렌더 라이브 확인). 폴리시 잔여: 숫자감각 콜드 랩 로딩 ~7초 동안 빈 화면(로딩 상태 없음), P1 퀴즈 회귀 스모크 — W3 검수 때 |
| W3 | P2 리플레이 미스터리 + 개념 도감 | ✅ 완료 — 브라우저 E2E (2026-07-13 Mac): 오토플레이 자가드로잉·스크러버 클램프·체크포인트 3종 판정(-1.55% 급락/VWAP/하루요약 -2.8%) 전부 정답판정·피날레 사건의 전말·전체수사→P1 퀴즈 회귀 통과·도감 vwap 수집(기록탭 1/10 UI+날짜 스탬프). 자동화탭 rAF 스로틀로 오토플레이 느림(실기기 정상, 스크러버 우회 가능 — 비차단) |
| W4 | P5 거시 도미노 + SRS-lite + P6 주말 리뷰 | ✅ 완료 — 브라우저 E2E (2026-07-13 Mac): 도미노 4/4 완주(+40XP) — 헤드라인=실제 캘린더 이벤트(Fed Bowman Speech), 노드별 실숫자(동결 66.3%·D-18 / 10Y 4.56%·2Y 4.21% / 스프레드 0.35%p / 인상 33.7%) + 경향 서술 메커니즘 + 피날레 "오늘의 숫자" 리캡. 주말 모드 = ET 일요일 라이브 상태로 검증(배너+덱 리뷰순 재배열). SRS = 오답 상태 주입 후 리로드 → 해당 개념이 복습 칩 달고 첫 문제 승격 확인. 참고: 자동화 탭 타이머 스로틀로 CountUp 정착 1~2초 지연 관찰(실기기 무관, W2 폴백이 보장). P5 도감 카드는 거시 용어집 부재로 스킵(사전 확장 시 배선) |
| W5-A | 프리미엄 비주얼 패스 (정답/오답 모멘트 · 오버레이 트랜지션 · 도미노 물리감 · 헌트/리플레이 연출 · 언락 드라마 · 덱 마이크로 · 이모지 0) | 🔨 구현·푸시됨 (검증 대기) — tsc 0 · FORBIDDEN 클린 · eslint 신규 에러 0(기왕증 5건 동일) · 전부 CSS 원샷(추가 rAF 0). 로컬 런타임 스모크는 이 머신의 Supabase env 부재(루트 레이아웃 가드)로 불가 → 시뮬 실화면 검증 필수 (아래 체크리스트) |

## W5-A 상세 (2026-07-13) — 프리미엄 비주얼 패스, 전부 `src/app/[locale]/wim/page.tsx` 단일 파일

> v1 최대 비판 "매력 없는 UX" 대응 — W1-W4 메커니즘을 "설문"이 아니라 "프리미엄 게임"으로
> 느끼게 하는 아트디렉션 패스. **엔지니어링 불변**: per-frame React state 애니메이션 0
> (iOS 웹뷰 리렌더 홍수 → 탭 불능 이력), 전부 CSS 키프레임/트랜지션 + 원샷 클래스,
> rAF는 CountUp 단독 유지. 키프레임은 단일 `WIM_KEYFRAMES` 블록(홈/퀴즈/플레이 트리당 1회
> 주입) + `prefers-reduced-motion: reduce`에서 전 애니메이션 즉시 종료 상태로 붕괴.

**디렉티브별 구현 (# = 지시 번호, 전부 구현)**:
1. **정답/오답 모멘트 (4플레이 + P1 퀴즈)**: `CorrectBurst` 신규 — 바이올렛 스트로크 8줄
   방사 버스트(SVG line 8개, wimBurst 450ms, forwards로 자체 소멸) + "+N XP" 24px 부상·페이드
   (wimXpFloat 700ms). 정답 버튼(퀴즈 +20/숫자감각·리플레이·도미노 +10)에 장착, 판정 칩은
   wimJudge 스프링(0.6→1.08→1, 350ms). 오답 = 선택 버튼 wimShake(3진동 300ms) +
   wimFlashAmber(앰버 아웃라인 플래시) — **오답 모멘트의 코랄(적색 계열) 전부 앰버로 교체**
   (버튼 보더/배경·판정 텍스트·✕아이콘, "NEVER red" 지시). 컨페티/이모지 0.
   레벨헌트는 이산 버튼이 없어 판정 칩에 버스트+스프링만 적용(XP는 칩 텍스트에 이미 내장 —
   중복 플로트 생략, 유일한 의도적 각색).
2. **열림/닫힘**: `PlayShell` 신규 — 4개 플레이 + P1 퀴즈 전부 래핑. 백드롭 페이드인 +
   시트 wimSlideUp(translateY 24px→0, 260ms cubic-bezier(0.22,1,0.36,1)); 닫기 =
   wimFadeOut 150ms 후 원샷 타이머로 언마운트(`closePlay`/`closeQuiz`, pointerEvents none).
   콘텐츠 스태거: 차트/히어로 카드 0ms → 질문 카드/선택지 +90ms(`both` 필). 주의: 슬라이드
   래퍼는 fill-mode 미사용 — transform 잔존 시 fixed 자식(글로서리 시트·토스트)의 containing
   block이 되는 버그를 회피(코드 주석 있음).
3. **도미노 물리감**: 커넥터를 트랙(P.line)+필(P.hero) 2층으로 재구성 — 노드 정답 시 필이
   scaleY 0→1로 **아래로 차오름**(transform-origin top, 400ms). 다음 카드는 열릴 때
   wimTilt(rotate -1.2deg→0 + translateY -6px→0, 300ms)로 "쓰러져 안착".
4. **레벨헌트 공개**: 실제 레벨 라인이 pathLength=100 dashoffset 100→0으로 **좌→우 자체
   드로잉**(wimDrawLine 500ms 드로우 후 디졸브) → 상주 대시라인+라벨이 0.42s에 페이드인 →
   내 추측선 wimPulseOnce(1→0.4→0.55) → 거리 칩 `CountUp(delay 450ms)`로 카운트업
   (CountUp에 `delay` prop 신규 — 기존 호출부 무변경).
5. **리플레이**: 선두 점에 wimHalo 펄스 헤일로(1.6s 루프, r9 서클 scale 1→2·opacity 0.4→0.05,
   transform-box fill-box); 체크포인트 정지 시 질문 카드가 아래에서 슬라이드인(wimUp 240ms) +
   차트 위 잉크색 오버레이 opacity 0→0.12 트랜지션 — "시간 정지" 감각.
6. **언락 드라마**: 첫 헌트 완주 → `unlockDrama` 상태 → 홈 복귀 시 풀스크린 모멘트:
   rgba(11,15,26,0.82) 스크림 페이드인 → 히어로 실세션 미니 차트가 중앙을 가로질러 드로잉
   (wimDrawKeep 550ms, 실데이터 없으면 합성 곡선 폴백) → MAX PAIN 대시라인 wimSweepIn +
   라벨 "MAX PAIN — 새 층 해제/New layer unlocked/新しい層を解放"(T 키 unlockDramaLabel,
   3언어 신규) → 1.4s에 wimDramaOut으로 히어로 방향 수축(translateY -30vh + scale 0.12) →
   1.7s 원샷 타이머가 기존 토스트로 핸드오프. **탭 즉시 스킵**(endUnlockDrama). CSS/SVG 단독.
7. **덱 마이크로**: 덱의 플레이 카드 + 사건 카드에 `.wim-press`(:active scale 0.97, 120ms);
   "신규" 칩에 `.wim-new::after` 시머 스윕(2.4s 스윕이 6s 주기 1회 = keyframe 0→40% 이동).
8. **이모지 스윕**: ICON_PATHS에 `flag` 스트로크 글리프 신규 → 퀴즈 마무리 버튼 `🏁` 제거.
   전 파일 이모지 레인지 스캔 결과 잔여 0 — 남은 ✓(U+2713)·✕(U+2715) 3건은 텍스트 글리프
   아이콘(지시상 허용), 화살표는 UI 텍스트 글리프.

**검증(정적)**: `tsc --noEmit` exit 0 · FORBIDDEN 정규식(`api/wim/today/route.ts:68` 정본)
전 파일 대조 통과 · 신규 이모지 0 · eslint 에러 5건 = W4와 동일한 기왕증(setState-in-effect
5곳), 신규 0 · 신규 rAF 0(CountUp delay 확장만). 로컬 런타임 스모크는 이 머신에 Supabase
env가 없어 루트 레이아웃 가드에서 전 페이지 크래시(WIM 무관 기왕증) → 불가, 시뮬로 확인.

**검증 절차 (시뮬 실화면, ko/en/ja 각각 `/{loc}/wim`) — 다음 세션에서 수행**:
- 모멘트: 퀴즈/숫자감각/리플레이/도미노에서 정답 시 버튼 중심 바이올렛 버스트 + "+N XP"
  부상 + 판정 스프링 팝; 오답 시 선택 버튼 3회 흔들림 + 앰버 플래시(**빨강 계열 0**),
  오답 상태색이 앰버로 보이는지. 레벨헌트 판정 칩 버스트/스프링.
- 열림/닫힘: 카드 탭 → 시트가 아래서 260ms 슬라이드업 + 카드 스태거(차트 먼저), 뒤로 →
  150ms 페이드 후 홈. 열림 직후·닫힘 중 탭 막힘 없는지(애니가 인터랙션 블로킹 금지).
- 도미노: 정답 → 커넥터가 아래로 차오르고(400ms) → 다음 도미노 탭 시 기울며 안착.
- 헌트: 확정 → 실제 레벨이 좌→우로 그려지고 대시라인으로 정착 + 추측선 1회 펄스 + 거리
  칩이 0.0→N.N% 카운트업(450ms 지연). 라운드 2+에서 카드 재애니 없음(정상).
- 리플레이: 선두 점 헤일로 맥동(과하지 않게), 체크포인트 정지 시 차트 살짝 어두워지고
  질문 카드 하단 슬라이드인, 답 후 디밍 해제.
- 언락 드라마(localStorage `wim.unlock.levels` 삭제 후 헌트 완주): 홈 복귀 → 스크림 →
  차트 드로잉 → MAX PAIN 스윕+3언어 라벨 → 히어로 쪽으로 수축 → 토스트. 중간 탭 = 즉시
  스킵+토스트. 이미 언락된 계정은 드라마 미발생.
- 덱: 카드 프레스 시 0.97 스케일, "신규" 칩 시머가 은은히 6초에 1회.
- 퀴즈 마지막 문제: 완료 버튼이 깃발 스트로크 아이콘(이모지 아님).
- 회귀: iOS 시뮬에서 헌트 드래그·리플레이 스크러버(touch-action) 정상, 글로서리 시트/도감
  토스트가 화면 기준 위치에 뜨는지(PlayShell transform 이슈 회귀 확인), 접근성 →
  설정에서 동작 줄이기 켜면 전부 즉시 상태.

## W4 상세 (2026-07-13) — 신규 API/프록시 0, 전부 `src/app/[locale]/wim/page.tsx` 단일 파일

**변경 (파일:컴포넌트)**:
1. **P5 `MacroDominoPlay` 신규** (풀스크린 오버레이, 기존 플레이와 동일 비주얼 언어): 데이터 =
   기존 공개 GET 3종을 클라이언트에서 직접 fetch — `/api/guardian/fedwatch`(ease/noChange/hike/
   daysUntilFomc), `/api/guardian/economic-calendar`(events[]), `/api/live/treasury`(10Y/2Y/30Y).
   프록시 라우트 불요(전부 same-origin). fedwatch·treasury 둘 다 실패 시 PlayEmpty.
   **헤드라인 카드**(바이올렛 그라디언트): 오늘/내일(ET) HIGH 임팩트 이벤트 → 없으면 fed 카테고리
   이벤트 → 없으면 FOMC 카운트다운(D-days + 동결확률 CountUp). 이벤트면 날짜·시각(ET)·임팩트 칩
   + 예상/이전 실수치 타일(CountUp, unit 표기). 라이브 확인: 2026-07-13 기준 픽 = 07-14 CPI
   (Inflation Rate YoY, HIGH, 예상 3.9% · 이전 4.2%).
   **도미노 체인 4노드** (탭→2지선다 개념 질문→정답판정→메커니즘 문장+오늘의 실수치 공개→다음
   도미노 언락, 커넥터 라인으로 연결): ① 기준금리→국채금리 (동결확률 %, 다음 FOMC D-day) ②
   국채금리→달러·할인율 (10Y·2Y 실수치) ③ 할인율→성장주 밸류에이션 (10Y−2Y 스프레드 %p) ④
   섹터 로테이션 (인상확률 %). 질문은 전부 시간 불변 개념형(항상 판정 가능 — "국채 금리가 오르면
   미래 이익의 현재가치는? 커진다/작아진다"), 메커니즘 카피는 전부 "경향/통상/과거에는" 서술형
   (예측 어휘 0, FORBIDDEN 정규식 전 파일 대조 — '매도' substring 트랩 포함 클린). 정답당
   +XP_TRIED(awardPlayXp), 오답 0(학습일 도트 유지). **피날레**: 사슬 완성 카드 + 점수/XP +
   "오늘의 숫자" 리캡 스트립(10Y/2Y/동결확률/다음 FOMC D-day, 로드된 것만).
   **도감 미연동(스킵)**: METRIC_GLOSSARY에 금리/거시 계열 MetricTerm이 없어(전 25종 옵션·수급·
   기술 지표뿐) 스펙 원칙(용어 발명 금지)에 따라 P5 완주는 도감 카드를 만들지 않음 — 거시 용어가
   글로서리에 추가되면 그때 11번째 카드로 배선.
2. **간격 반복 SRS-lite**: `localStorage 'wim.srs'` = {termId:{wrong,right,last:dateET}}.
   기록 지점 = 숫자감각 전 문항(문항 지표 용어), 레벨헌트 전 라운드(레벨 용어, ≤3%=right),
   리플레이 체크포인트(b)(vwap). P1 퀴즈는 스킵(무변경). **우선순위 규칙**: wrong>right AND
   last≠오늘(ET)인 용어의 문항을 셔플 후 안정 파티션으로 앞으로 — 세션당 최대 2문항 캡(벌칙 루프
   방지), 같은 용어 중복 승격 없음. 승격 문항 카드에 앰버 "복습/Review/復習" 칩.
3. **P6 주말 리뷰 모드** (배너+재정렬만, 신규 플레이 없음 — 스펙 최소가동형): ET 기준 토/일
   판정(Intl America/New_York, 서버 lastTradingDayET와 동일 매핑). 주말이면 ① 홈 히어로 CTA 위
   컴팩트 "주말 리뷰" 배너(3언어, 앰버 아이콘+흰 반투명 로우) ② 플레이 덱 재정렬: 리플레이 →
   숫자감각(SRS 우선순위가 자연히 복습 태움) → 레벨헌트 → 거시도미노가 덱 앞, 사건 퀴즈 카드가
   뒤. 평일은 기존 순서(퀴즈 먼저) 무변경.
4. **배관**: ICON_PATHS `chain`(체인 링크) 추가; 덱 4번째 플레이 카드(거시 도미노, "신규" 칩,
   플레이 카드 4장은 `playCards` 공용 렌더로 통합·주말 랭크 정렬); playOpen 유니언 +'domino';
   `etTodayStr`/`isWeekendET` 헬퍼; `srsRecord`/`isReviewDue` 콜백(3 플레이에 prop 배선);
   T dict 신규 33키 ko/en/ja 전부 네이티브. eslint 신규 에러 0(기왕증 5건 동일 — headEvent를
   effect 내 계산으로 옮겨 impure-during-render 신규 1건 해소).

**검증(정적)**: `tsc --noEmit` exit 0 · FORBIDDEN 정규식 전 파일 대조 통과 · 신규 이모지 0 ·
방향 색칠 0(스탯 타일 전부 바이올렛/앰버 중립) · 로직 노드 어서션 통과(SRS due/캡2/중복차단/
기록 산식, 헤드라인 픽 폴백 순서, 스프레드 반올림, 주말 덱 순서) · 라이브 3 엔드포인트 실측
(fedwatch noChange 66.3/D-18, treasury 4.56/4.21/5.06, calendar 07-13~14 25건 HIGH 7건).

**검증 절차 (시뮬 실화면, ko/en/ja 각각 `/{loc}/wim`) — 다음 세션에서 수행**:
- 덱: 4번째 카드 "거시 도미노/Macro Domino/マクロドミノ"+신규 칩, 탭 시 오버레이.
- 도미노: 헤드라인 카드에 실제 이벤트(07-14 CPI HIGH 예상 3.9%·이전 4.2%) 또는 FOMC D-day가
  CountUp으로 뜨는지, 체인 노드가 잠금(대시)→탭해서 열기→질문→판정(+10XP/0)→메커니즘+실수치
  타일→커넥터 바이올렛 점등→다음 노드 순으로 진행되는지, 4노드 후 피날레(점수+오늘의 숫자
  리캡: 10Y 4.56%·2Y 4.21%·동결확률 66.3%·D-18), 닫기→홈 XP 반영.
- 도미노 열화: fedwatch/treasury 차단 시 PlayEmpty(빈 화면 금지), 캘린더만 실패 시 FOMC
  카운트다운 헤드라인으로 폴백.
- SRS: 숫자감각에서 일부러 오답 → localStorage wim.srs에 wrong 증가 확인 → (ET 날짜 바꾸거나
  last 수동 수정 후) 재입장 시 해당 용어 문항이 앞으로 오고 "복습" 칩 표시, 같은 세션 최대 2문항,
  같은 날 재입장 시 우선순위 미발동. 레벨헌트 라운드·리플레이 vwap 체크포인트도 wim.srs에 기록.
- 주말 모드(시뮬: 시스템 시계 또는 isWeekendET 강제): 히어로 CTA 위 "주말 리뷰" 배너 3언어,
  덱 순서 리플레이→숫자감각→레벨헌트→도미노→퀴즈들. 평일엔 배너 없음+기존 순서.
- 회귀: P1 퀴즈/레벨헌트/숫자감각/리플레이 정상 동작(onSrs 추가로 인한 크래시 없음), 도감 수집
  토스트, 언락 히어로 MAX PAIN 라인.

## W3 상세 (2026-07-13) — 신규 API 0, 전부 `src/app/[locale]/wim/page.tsx` 단일 파일

**변경 (파일:컴포넌트)**:
1. **P2 `ReplayPlay` 신규** (풀스크린 오버레이, 레벨헌트와 동일 비주얼 언어): 히어로 유닛의
   `unit.spark`(세션 5분봉 closes+vwap) 사용 — 히어로 스파크 <60봉이면 ≥60봉인 첫 유닛으로
   폴백, 없으면 PlayEmpty. **차트가 스스로 그려짐**: 35ms/봉 오토플레이 + 재생/일시정지 버튼 +
   하단 스크러버(레벨헌트와 동일 iOS-safe 패턴: touch-action none + setPointerCapture).
   가격 도메인은 전체 세션 고정(리스케일 점프 없음 — 전부 종료된 데이터라 컴플라이언스 무관).
   **체크포인트 2~3개, 전부 클라이언트 계산**: (a) 세션 최대 5분 변화(|close[i]-close[i-1]|
   argmax, 마지막 2봉 제외) → "방금 무슨 일이?" 급락/급등/거의안움직임 (판정: |변화|<0.2%→
   무변화, 아니면 부호), (b) (a) 이후 첫 지속 VWAP 크로스(부호 반전+3봉 유지; 크로스 없으면
   세션 72% 지점 폴백, 가격-VWAP 갭 ≥0.05%인 봉만 채택 — 동전던지기 방지, 갭 없으면 스킵)
   → "이 시점 가격은 VWAP 대비?" 위/아래, (c) 마지막 봉 → "하루를 한 줄로?" 크게오른날/
   크게내린날/조용한날 (첫봉 대비 마지막봉 ±1% 기준). 스크럽으로 체크포인트를 지나치려 해도
   maxVi 클램프로 미답 체크포인트에서 강제 정지 → 질문은 항상 "이미 그려진" 구간만 다룸.
   정답당 +XP_TRIED(awardPlayXp), 오답 0(학습일 도트는 유지). 답한 체크포인트는 차트에 앰버
   점 마커로 잔존. **피날레**: 차트 완성 후 `unit.explanation[loc]` "사건의 전말" 카드(Bold
   파서 재사용) + 점수/XP 칩 + "전체 수사 열기" 링크 버튼(리플레이 닫고 해당 유닛 startQuiz).
2. **개념 도감(Almanac)**: `ALMANAC_TERMS` 10종 = maxPain·callWall·putFloor·vwap·darkPool·
   pcr·shortInterest·gammaFlip·rsi·trendPhase(=SMA). **수집 조건**(최초 1회): ① 플레이 최고
   판정 — 레벨헌트 라운드 "대박"(≤1%)→해당 레벨 용어 / 숫자감각 정답→문항 지표 용어(dark→
   darkPool, pcr→pcr, short→shortInterest, sma→trendPhase; vol→volRegime은 도감 밖이라 무시)
   / 리플레이 체크포인트(b) 정답→vwap, ② 해당 용어 GlossarySheet 열람(markTerm 훅).
   수집 시 `localStorage 'wim.almanac'` = {termId: {dateET, ticker, closes(히어로 스파크
   ~40pt 다운샘플, 소수 2자리)}} + 2.5초 토스트 "도감에 새 카드 — {용어명}"(3언어, 모든 화면
   에서 렌더 — 플레이/퀴즈/홈). **UI**: 기록(Me) 탭 최상단 2열 그리드 — 수집 카드 = 날짜 칩 +
   MiniSpark(저장 closes) + 용어명 + 티커, 탭 시 GlossarySheet; 미수집 = 대시 보더 + lock
   (비인터랙티브 — 도감에서 바로 열어 수집하는 치트 차단). 카운터 N/10.
3. **폴리시(W2 검증 잔여)**: `PlayLoading` 신규(스피너 + "실데이터 불러오는 중" 3언어) —
   숫자감각·레벨헌트의 콜드 랩 로딩 시 기존 셔머 스켈레톤이 라벤더 배경에서 사실상 안 보이던
   문제 해소. wimSpin 키프레임 추가.
4. **배관**: ICON_PATHS `replay`(되감기)/`pause` 추가; 덱에 3번째 플레이 카드(리플레이,
   "신규" 칩, openPlay('replay')); playOpen 유니언 확장; T dict 신규 26키 ko/en/ja 전부.

**검증(정적)**: `tsc --noEmit` exit 0 · FORBIDDEN 정규식 전 파일 대조 통과(신규 카피 전부
과거형/서술형 — '쇼트' 표현 유지, '매도' substring 트랩 없음) · 신규 이모지 0(🏁 1건은 W1
이전 기왕증) · 체크포인트 산식 노드 시뮬 3케이스(급락+크로스/vwap無/완전평탄) 어서션 통과.

**검증 절차 (시뮬 실화면, ko/en/ja 각각 `/{loc}/wim`) — W2 잔여 스모크 포함**:
- 덱: 3번째 카드 "리플레이 미스터리/Replay Mystery/リプレイミステリー"+신규 칩, 탭 시 오버레이.
- 리플레이: 차트가 자동으로 그려지는지(~35ms/봉), 일시정지/재생 토글, 스크러버 드래그 중
  페이지 스크롤 안 되는지(**iOS 시뮬 필수**), 체크포인트에서 자동 정지+질문 카드, 스크럽으로
  미답 체크포인트 통과 불가(클램프), 정답 +10XP·오답 0, 답한 지점 앰버 마커, VWAP 대시라인+
  범례, 마지막 질문 후 "사건의 전말"+설명 텍스트, "전체 수사 열기"→해당 유닛 퀴즈 열림,
  스파크 없는 날 PlayEmpty.
- 도감: 기록 탭 최상단 그리드 10칸+카운터, 미수집=lock 대시 카드(탭 무반응), 레벨헌트 대박/
  숫자감각 정답/리플레이 vwap 정답/사전 열람 각각으로 수집되는지, 수집 직후 2.5초 토스트
  (플레이 화면 위에서도), 새로고침 후 카드 유지(localStorage), 수집 카드 탭→GlossarySheet,
  카드에 날짜·티커·미니차트 표시.
- 숫자감각/레벨헌트: 콜드 로드에서 스피너+"실데이터 불러오는 중"이 보이는지(빈 화면 금지).
- **W2 잔여 스모크**: P1 퀴즈 회귀(히어로/덱 카드 → 풀이 → 정답/오답 → 딥레이어 → 복귀,
  XP·주간 도트 반영), 언락 토스트/히어로 MAX PAIN 라인 회귀 없음.

## W2 상세 (2026-07-13) — 신규 API 0, 전부 `src/app/[locale]/wim/page.tsx` 단일 파일

**변경 (파일:컴포넌트)**:
1. **P3 `LevelHuntPlay` 신규** (풀스크린 오버레이, 퀴즈 오버레이와 동일 비주얼 언어):
   히어로 유닛 티커의 lab 스냅샷 사용. 라운드 = maxPain→callWall→putFloor 중 non-null만
   (셋 다 null 또는 스파크 부재 → "오늘은 이 데이터가 없어요" 빈 상태, XP 0).
   대형 차트(264px, 측정폭 1:1 viewBox — 왜곡 없음) 위 수평 추측선을 포인터 드래그
   (touch-action:none + setPointerCapture — iOS 웹뷰에서 스크롤 안 끌림), 우측 플로팅 $칩.
   가격 도메인 = closes ∪ 전체 레벨 +8% 패딩 **고정** (라운드별 리스케일로 힌트 누설 방지).
   확정 → 실제 레벨 대시라인+라벨 공개, 근접도 판정(≤1% +XP_CORRECT "대박" / ≤3% +XP_TRIED /
   그 외 0 + 격려 카피) + 개념 한 줄 의미(3언어). 이미 찾은 레벨은 다음 라운드 차트에 잔존
   (차트가 자라는 감각). 최종 요약 = 라운드별 차이% + 합계 XP.
2. **P4 `NumberSensePlay` 신규**: 오늘 유닛 티커 최대 3개의 lab을 병렬 fetch(캐시 우선),
   non-null 필드로 질문 풀 구성 — darkPoolPct vs 50 · pcr vs 1.0 · shortVolPct vs 40 ·
   vol.regimeScore vs 50 · 종가 vs SMA50 (동률 문항 제외). 셔플 후 5문항(3개 미만이면 빈 상태).
   높다/낮다 큰 버튼 2개(chevUp/chevDown 아이콘 — 방향 색칠 없음, 둘 다 바이올렛) →
   즉시 실값 CountUp 공개 + 한 줄 의미 + 러닝 스코어. 정답당 +XP_TRIED, 5/5 보너스 +XP_CORRECT.
3. **오버레이 언락 1단계**: 레벨 헌트 최초 완주 → `localStorage 'wim.unlock.levels'='1'` →
   홈 히어로 RealChart에 MAX PAIN 대시라인(#FFD66B, lab.levels.maxPain non-null일 때만) 상시
   렌더 + 요약 화면 언락 배지 + 홈 복귀 시 4초 토스트 "차트에 새 층이 열렸어요 — 맥스페인"(3언어).
   재방문 유저는 부팅 시 플래그 복원 → 히어로 lab lazy fetch.
4. **배관**: 단일 `lab` state → `labs` Record(티커별) 캐시 + `requestLab`(in-flight 중복 제거);
   `heroU` 계산 훅 위로 호이스트(플레이·홈 공용); `awardPlayXp`(XP+주간 도트 — record()와 동일
   규칙); W1 티저 2장 → 실 버튼 배선("곧 열림"→"신규"+풀기 칩, lock 제거); 공용 `PlayTopBar`/
   `PlayEmpty`; ICON_PATHS `chevUp`/`chevDown` 추가; T dict 신규 ~33키 ko/en/ja 전부.

**검증(정적)**: `tsc --noEmit` exit 0 · FORBIDDEN 정규식 전 파일 대조 통과(ko "공매도"가
substring '매도'에 걸려 "쇼트(빌려서 파는 거래)"로 재표현) · 신규 이모지 0 · 예측/조언 어휘 0
(모든 카피 과거형/서술형 — 오늘 이미 계산된 레벨/수치만 다룸).

**검증 절차 (시뮬 실화면, ko/en/ja 각각 `/{loc}/wim`) — W1 잔여 포함**:
- 덱 끝 2장이 "신규/NEW/新着"+풀기 칩의 살아있는 카드인지, 탭 시 각 오버레이 열림.
- 레벨 헌트: 차트 드래그 중 페이지가 스크롤되지 않는지(**iOS 실기기/시뮬 필수**), 칩 $값
  실시간 갱신, 확정 → 대시라인+판정+한줄 의미, null 라운드 스킵, 요약 후 첫 완주 언락 배지 →
  홈 토스트 → 히어로 차트에 MAX PAIN 라인 상시(새로고침 후에도).
- 숫자 감각: 5라운드(데이터 부족 시 3+), 높다/낮다 → 실값 카운트업+기준 표시+러닝 스코어,
  5/5 보너스 칩, 요약 XP.
- 데이터 없는 날/티커: 두 플레이 모두 빈 상태 카드 + XP 미지급.
- 플레이 XP가 기록 탭 XP/주간 도트에 반영되는지.
- W1 잔여: en/ja 문자열 전반(홈·플레이), 스트릭 1줄 밴드, 퀴즈 회귀(풀이→정답→딥레이어→복귀).

## W1 상세 (2026-07-13) — 홈 탭만, 퀴즈/사전/검색/기록 탭·API 무변경

**변경 (전부 `src/app/[locale]/wim/page.tsx` 단일 파일)**:
1. **차트 히어로 강화** (`WimPage` 홈 히어로 섹션): RealChart 116→150px + 카드 좌우 풀블리드
   (`margin -16px`, 가격 라벨 유지). ±% 배지에 `CountUp` 신규 컴포넌트(rAF 0→값 600ms
   ease-out, 라이브러리 無; 종료 시 원본 값 그대로 렌더). 차트 아래 `SessionStrip` 신규
   컴포넌트: PRE·REG·POST 3분절 히트 스트립, `unit.session` 필드(현 API는 'REG' 고정)로
   해당 세션 하이라이트, 필드 부재 시 REG 기본. `Unit`에 `session?: string` 추가.
2. **오늘의 플레이 덱** (기존 "사건 파일" 레일 대체): 카드 168→212px 확대(snap 유지),
   로고 28px + `MiniSpark`(w/h prop 추가, 뷰박스 기반이라 왜곡 없음) + ±% + 난이도 칩 +
   풀기 칩 — 탭 동작(startQuiz) 동일. 덱 끝에 W2 티저 2장(레벨 헌트=crosshair 아이콘,
   숫자 감각=updown 아이콘 — ICON_PATHS 신규 2개) + "곧 열림" 칩, **비인터랙티브 div**
   (대시 보더 + lock 아이콘, W2에서 배선).
3. **스트릭 밴드 압축**: 3줄(링+주간 도트+레벨/XP바) → 1줄(링 44px + "이번 주 N일" +
   XP 칩 인라인). 주간 도트·레벨 진행바는 기록(Me) 탭에 그대로 존재.
4. **i18n**: 신규 문자열 8종(playDeck/comingSoon/teaserHunt(+Sub)/teaserSense(+Sub)/
   sessionPre·Reg·Post) ko/en/ja 전부 T dict 추가. 이모지 0, 방향 색칠 0(크기만),
   예측·조언 어휘 0(FORBIDDEN 정규식 대조 확인), 면책 푸터 무변경.

**tsc --noEmit exit 0** (eslint 에러 2건은 기존 부트/타임아웃 이펙트 — W1 무관 기왕증).

**검증 절차 (시뮬 실화면, ko/en/ja 각각 `/{loc}/wim`)**:
- 히어로: 차트가 카드 좌우 끝까지 + 세로 확대, 고/저 가격 라벨 표시, ±% 숫자가 진입 시
  0→값으로 굴러 올라감, 차트 아래 PRE·REG·POST 스트립에서 본장(REG)만 점등.
- 플레이 덱: 헤더 "오늘의 플레이/Today's plays/今日のプレイ", 큰 카드 스냅 스크롤,
  각 카드 실스파크+난이도 칩, 탭 시 기존 퀴즈 열림. 덱 끝 티저 2장(레벨 헌트·숫자 감각)
  "곧 열림/Coming soon/近日公開" 칩 + 탭해도 무반응인지.
- 스트릭: 한 줄 밴드(링+이번 주 N일+XP 칩)만 있고 주간 도트가 홈에서 사라졌는지,
  기록 탭에는 도트·레벨바 그대로인지. 퀴즈 풀이→정답→딥레이어→홈 복귀 회귀 확인.

## W0 상세 (2026-07-13)

**진단 (실측)**: 일요일 ET 아침 방문자가 `wim:units:v2:2026-07-12` 키로 생성 트리거 →
주말이라 gainers/losers 무버 빈 결과 → 열화 유닛 3개(가치無) → 홈 히어로 카드 빈 렌더.
+ WIM은 워밍 크론이 없는 pull 구조(UC와 동일 결함이었음).

**수정 (파일:함수)**:
1. `api/wim/today/route.ts` — `lastTradingDayET()` 신설: ET Sat→-1일, Sun→-2일.
   캐시 키·aggs 범위 끝·시드 전부 이 값 사용 → **주말엔 금요일 정식 캐시를 서빙**
   (열화 생성 경로 자체 차단). 공휴일은 미매핑 — value 무버가 전세션 데이터를 들고 있어
   커버됨, 공휴일 빈화면이 실제 관측되면 그때 확장 (스펙 §5 주석 참조).
2. `api/cron/uc-warm/route.ts` — `warmWim()` 추가: 일반 GET(콜드면 인라인 생성) →
   `_stale`(>4h)일 때만 `refresh=1` 재생성. **매 사이클 refresh 강제 금지** —
   유닛 AI는 8k토큰 호출이라 15분마다 재생성하면 동일 콘텐츠에 돈만 탐. 병렬 웨이브에 편입.
3. 클라이언트 가드: 근본 차단으로 불요 판단 (열화 유닛이 더는 생성 안 됨). W1 재설계에서
   히어로가 어차피 재작성됨.

**검증 절차 (이 파일 갱신 시점 기준)**: tsc → push → 프로드 `api/wim/today` 응답의
dateET가 주말에도 금요일인지 + units 5개·moveMagnitude 정상 → 시뮬 `/ko/wim` 히어로 렌더 확인.

## 다음 세션 시작법

1. `git pull` → 이 파일 + `WIM_V2_SPEC.md` 읽기
2. 상태표에서 ⬜ 첫 페이즈 착수 (스펙의 해당 절이 요구사항)
3. 완료 기준: push + 시뮬 3언어 실화면 + 이 파일 상태 갱신 커밋
