# REVIEW 2 — DS 파운데이션 v2.1 (2차 검토)

> 1차 패치 병합 확인: `--f-mono`/글로우/이징 토큰이 `app-tokens.css`에 들어갔습니다. 👍
> 이번 회차: 파운데이션 시트 + 룰 1(티커 아이콘) + 룰 2(깊이) + 드리프트 2건.
> 시각 레퍼런스: **SIGNUM HQ — Design System.html** (파운데이션) · **SIGNUM HQ — App v2.html** (적용 시안)

## 1. 드리프트 — 복원 권장 🔴

| 토큰 | 현재 (stock2) | 스펙 | 영향 |
|---|---|---|---|
| `--bg` | `#0b111e` | **`#050a14`** | 캔버스가 밝아져 코스믹 깊이·글로우 대비 약화 |
| `--bg-elev` | `#121926` | `#0a1120` | 동일 |
| `--text-dim` | `#e2e8f0` | **`#94a3b8`** | `--text(#f8fafc)`와 거의 동일 → 2단계 위계 소멸 |
| `--text-muted` | `#94a3b8` | **`#475569`** | 라벨/캡션이 본문과 경쟁 |

텍스트 3단계(#f1f5f9 / #94a3b8 / #475569)가 "밀도 높지만 안 어지러운" 터미널 위계의 핵심입니다.
가독성 때문에 밝혔다면 muted만 `#64748b`로 한 단계 타협하는 것을 권장.

## 2. 룰 1 — TickerIcon (`05-TickerIcon.tsx`)
- 18종 등록(지수·매크로·개별주), 미등록 티커는 회색+이니셜 폴백
- 브랜드 휴 그라데이션 + 모노그램 = 상표 로고 복제 아님 (라이선스 안전)
- 실제 로고가 필요하면 Phase 2에서 parqet/clearbit 이미지로 교체 가능하게
  `<TickerIcon>` 인터페이스 유지 — 호출부는 안 바뀜

## 3. 룰 2 — 깊이 3단계 (`06-ds-v21.css`)
- `--depth-1/2/3` + `--inner-glow-cyan` 토큰 추가
- 매핑: 리스트 셀/타일 → depth-1 · 일반 카드 → depth-2 · 히어로(가격·Eye·HUD) → depth-3+이너글로우
- 모든 카드에 `inset 0 1px 0 rgba(255,255,255,0.05~0.09)` 상단 하이라이트 — 유리 두께감의 핵심

## 4. 적용 순서
1. [ ] 드리프트 복원 (§1) — `app-tokens.css` 4개 토큰
2. [ ] `06-ds-v21.css`의 `:root` 블록을 `app-tokens.css`에 병합, 깊이 매핑을 각 module.css에
3. [ ] `05-TickerIcon.tsx` → `src/components/app/TickerIcon.tsx`, 파일 하단 주석의 위치에 적용
4. [ ] 390×844에서 텍스트 겹침 검수 (시안 게이지처럼 숫자는 게이지 아래 배치)
